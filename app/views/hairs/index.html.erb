
<div class="top-bar-profile">
  <img src="/assets/logo.jpg" class="logo-img"/> 
  <%= link_to('Logout', destroy_user_session_path, method: :delete, :class => 'profile-logout') %>
</div>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @hairs.each do |hair| %>
      <tr>
        <td><%= link_to 'Show', hair %></td>
        <td><%= link_to 'Edit', edit_hair_path(hair) %></td>
        <td><%= link_to 'Destroy', hair, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>

  <div class="profile-area"> 
    <div class="user-data">
      <!-- fazer a rederização condicional aqui, para puxar os ico certo-->
      <img class="user-data-icon" src="/assets/null_icon.png" /> 
      <a class="set-data">Set data</a>
      <img class="user-data-icon" src="/assets/null_icon.png" /> 
    </div>
    <a href="" class="generate-hc">Generate haircuts</a>
  </div>
</table>


